apply from: rootProject.file('plugin/eureka-client.gradle')

// 依赖
dependencies {
    compile 'org.springframework.cloud:spring-cloud-starter-openfeign'
    compile 'io.github.openfeign:feign-gson'
}
